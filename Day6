#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */  
    int x;
    
    cin>>x;

    for(int i=0;i<x;i++)
     {   
      string a;
      getline(cin >> ws, a);
    
      for(int j=0;j<a.size();j=j+2)
       { cout<<a[j];
       }
      
      cout<<" ";

       for(int k=1;k<a.size();k=k+2)
       { cout<<a[k];
       }
      
      cout<<endl; 
    } 
    return 0;
}
